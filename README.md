# Aufgabe E1 – isEven (Gruppe B – TDD, keine KI)

In dieser Aufgabe üben Sie **Test-Driven Development (TDD)** an einer kleinen Funktion, die prüft, ob eine Zahl gerade ist.

## Aufgabe

Implementieren Sie in der Klasse `IsEven` die Methode

```java
public static boolean isEven(int n)